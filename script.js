// animating the static & making the animation occur only when we hover the card this can require js to modify the seed value over time or we can use animate element in html 

// optional js :

// const turbulence = document.querySelector("#noise feTurbulence");

// document.querySelector("#card").onmouseenter = () => {
//     setInterval(() => {
//         turbulence.setAttribute("seed", (Math.random() * 1000));
//     }, 10);  
// }